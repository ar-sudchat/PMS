-- ============================================
-- STEP 2: ทดสอบ Dry-Run (ไม่บันทึกจริง)
-- รันไฟล์นี้เพื่อทดสอบก่อน import จริง
-- ============================================

-- Dry-run: ทดสอบโดยไม่บันทึกข้อมูลจริง
EXEC pms.sp_batch_import_legacy_timesheet @year = 2026, @dry_run = 1;
GO
